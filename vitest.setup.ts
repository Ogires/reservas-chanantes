// Matchers de jest-dom para las pruebas de componentes (Testing Library).
// Solo extiende `expect`; es inocuo en los tests de entorno `node`. La limpieza
// del DOM la auto-registra @testing-library/react por fichero al importarse.
import '@testing-library/jest-dom/vitest'
